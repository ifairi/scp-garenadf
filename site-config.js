/* Generated values are injected into dist/site-config.js during `npm run build`.
   Keep this source fallback empty so the existing static content stays available. */
window.SCP_SUPABASE = Object.freeze({
  url: '',
  publishableKey: ''
});
